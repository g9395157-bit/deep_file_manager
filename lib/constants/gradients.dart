import 'package:flutter/material.dart';
import 'colors.dart';

/// Ember gradient used across hero elements
const LinearGradient kEmberGradient = LinearGradient(
  colors: [kAmber, kOrange],
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
);
